services:
  backend:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: watchparty_backend
    restart: always
    ports:
      - "5001:5001"
    env_file:
      - ./server/.env
    environment:
      PORT: 5001
